<%= form_for(meddage) do |f| %>
  <% if meddage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meddage.errors.count, "error") %> prohibited this meddage from being saved:</h2>

      <ul>
      <% meddage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
